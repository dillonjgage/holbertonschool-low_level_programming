#include "main.h"
/**
 * main - will return the name of the executable file only
 * @argc: the number of args given to the program
 * @argv: an array of pointers to the args
 *
 * Return: 0
 */
int main(int __attribute__((__unused__)) argc, char* argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
